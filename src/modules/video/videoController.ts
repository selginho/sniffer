class VideoController {
  public async upload (req: Request, res: Response): Promise<Response> {
    return res.json()
  }

  public async notifyUpload (req: Request, res: Response): Promise<Response> {
    return res.json()
  }
}

export default new VideoController()
